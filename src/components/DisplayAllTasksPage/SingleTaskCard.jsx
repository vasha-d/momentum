


export default function SingleTaskCard (taskObj) {

    const {
        name,
        description,
        due_date,
        status_id,
        employee_id,
        priority_id
        } = taskObj
    
    return (
        <div>
            h
        </div>
    )
}